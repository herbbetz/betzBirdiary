testmode = False

if len(sys.argv) > 1 and sys.argv[1] == "test":
    testmode = True
    ms.log(f"{sys.argv[0]} running in test mode")
else:
    ms.log(f"Starting {sys.argv[0]} at {datetime.now()}")



signal_logger = None
trace = None

if testmode:
    signal_logger = SignalLogger()
    trace = TraceRecorder()

if testmode:
    trace.dump_event(
        "BOOT",
        sample
    )
if testmode:
    signal_logger.log(
        sample,
        event
    )

------------------------OR MAYBE BETTER:
class NullRecorder:
    def dump_event(self, *args, **kwargs):
        pass

    def log(self, *args, **kwargs):
        pass

if testmode:
    signal_logger = SignalLogger()
    trace = TraceRecorder()
else:
    signal_logger = NullRecorder()
    trace = NullRecorder()

After that the main loop stays unchanged:
trace.dump_event(...)
signal_logger.log(...)